import type { AgentTraceStep } from '../../types';
interface AgentTraceProps {
    steps: AgentTraceStep[];
    currentIndex: number;
}
export default function AgentTrace({ steps, currentIndex }: AgentTraceProps): import("react").JSX.Element;
export {};
